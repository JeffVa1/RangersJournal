---
interface Props {
  title: string;
  class?: string;
}

const { title, class: className = "" } = Astro.props;
---

<section class:list={["character-sheet-section", className]}>
  <h3>{title}</h3>
  <slot />
</section>
